require 'spec_helper'

describe AttendeaseSdk do
  it 'has a version number' do
    expect(AttendeaseSdk::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
